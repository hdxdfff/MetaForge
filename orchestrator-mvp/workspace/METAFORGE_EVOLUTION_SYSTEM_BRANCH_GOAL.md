# MetaForge Evolution System Branch Goal

Status: frozen until Self-Improvement Engine proves the patch-test-merge loop.

Scope: only `D:\codex\orchestrator-mvp`

## Freeze rule

Evolution System is not an active primary execution line right now.

It may only:

- preserve existing evaluation state
- emit report-only notes
- define future constraints for safe rollout

It must not become a primary build-out track until MetaForge can automatically:

1. find bug
2. generate patch
3. pass tests
4. merge code

## Why frozen

Without a working Self-Improvement loop, variant selection and mutation become empty motion instead of real organizational improvement.

## Allowed mode

Report-only mode.

Allowed outputs:

- future trial schemas
- rollout safety notes
- selection metric drafts

Disallowed as primary work:

- active variant spawning as a top-level goal
- population-loop expansion on the main lane
- consuming the main factory lane
